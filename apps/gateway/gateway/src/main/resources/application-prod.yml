spring:
  config:
    activate:
      on-profile: prod
  cloud:
    gateway:
      routes:
        - id: auth
          uri: http://hr-auth:80
          predicates:
            - Path=/api/auth/**,/api/auth/password-reset/request
          filters:
            - StripPrefix=1

        - id: hr-public
          uri: http://hr-backend:80
          predicates:
            - Path=/api/v1/company-applications/submit,/api/v1/health
          filters:
            - StripPrefix=2

        - id: hr-files
          uri: http://hr-backend:80
          predicates:
            - Path=/api/v1/files/**

        - id: hr
          uri: http://hr-backend:80
          predicates:
            - Path=/api/v1/**
          filters:
            - StripPrefix=2
            - JwtAuthenticationFilter

        - id: notification-api
          uri: http://hr-notification:80
          predicates:
            - Path=/api/notifications/**
          filters:
            - StripPrefix=1
            - JwtAuthenticationFilter

        - id: chatbot
          uri: http://hr-chatbot:80
          predicates:
            - Path=/api/chat/**
          filters:
            - StripPrefix=1
            - JwtAuthenticationFilter

jwt:
  secret-key: ${JWT_SECRET_KEY}

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS}
  allowed-methods: ${CORS_ALLOWED_METHODS}
  allowed-headers: ${CORS_ALLOWED_HEADERS}
  allow-credentials: ${CORS_ALLOW_CREDENTIALS}

internal:
  token: ${INTERNAL_TOKEN}
