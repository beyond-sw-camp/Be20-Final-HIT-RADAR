<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.hit.hradar.domain.evaluation.query.mapper.EvaluationSectionMapper">

    <select id="findByCycleEvalTypeId"
            parameterType="long"
            resultType="org.hit.hradar.domain.evaluation.query.dto.response.row.EvaluationSectionRow">

        SELECT
            section_id          AS sectionId,
            section_title       AS sectionTitle,
            section_description AS sectionDescription,
            section_order       AS sectionOrder
        FROM evaluation_section
        WHERE cycle_eval_type_id = #{cycleEvalTypeId}
          AND is_deleted = 'N'
        ORDER BY section_order ASC

    </select>

</mapper>