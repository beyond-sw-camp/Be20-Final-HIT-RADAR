<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.hit.hradar.domain.leave.query.mapper.LeaveGrantMapper">

  <select id="findByGrantId"
    resultType="org.hit.hradar.domain.leave.query.dto.response.LeaveGrantDto">
    SELECT
      grant_id       AS grantId,
      emp_id         AS empId,
      year,
      total_days     AS totalDays,
      remaining_days AS remainingDays,
      granted_days   AS grantedDate,
      expire_date    AS expireDate
    FROM leave_grant
    WHERE grant_id = #{grantId}
      AND is_deleted = 'N'
  </select>

  <select id="findByEmpId"
    resultType="org.hit.hradar.domain.leave.query.dto.response.LeaveGrantDto">
    SELECT
      grant_id       AS grantId,
      emp_id         AS empId,
      year,
      total_days     AS totalDays,
      remaining_days AS remainingDays,
      granted_days   AS grantedDate,
      expire_date    AS expireDate
    FROM leave_grant
    WHERE emp_id = #{empId}
      AND is_deleted = 'N'
    ORDER BY year DESC
  </select>

</mapper>
